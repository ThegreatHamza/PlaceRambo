import SellForm from "@/components/sell/sell-form";

export const metadata = {
  title: "Post a listing — PlaceRambo",
  description: "Post a free listing on PlaceRambo, Djibouti's premium marketplace.",
};

export default function SellPage() {
  return <SellForm />;
}
