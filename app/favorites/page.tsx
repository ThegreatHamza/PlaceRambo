import FavoritesList from "@/components/favorites/favorites-list";

export const metadata = { title: "Favorites — PlaceRambo" };

export default function FavoritesPage() {
  return <FavoritesList />;
}
